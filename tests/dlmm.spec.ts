import "./quote.test";
import "./fuzz/liquidity_swap_fuzz.test";

describe("dlmm — integration (requires local validator)", () => {
  it("placeholder: test suite ready for on-chain deployment", () => {
    // See tests/integration/ for skeleton
  });
});
